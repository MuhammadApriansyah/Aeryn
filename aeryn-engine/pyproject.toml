[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "aeryn-engine"
version = "0.1.0"
description = "Aeryn Engine — High-performance Rust modules for Aeryn"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
